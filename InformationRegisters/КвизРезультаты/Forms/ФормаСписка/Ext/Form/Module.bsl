
&НаСервереБезКонтекста
Функция СписокПередУдалениемНаСервере(Параметры)
	Попытка
		НЗ = РегистрыСведений.КвизРезультаты.СоздатьНаборЗаписей();
		НЗ.Отбор.Период.Установить(Параметры.Период);
		НЗ.Отбор.Вопрос.Установить(Параметры.Вопрос);
		НЗ.ОбменДанными.Загрузка	= Истина;
		НЗ.Записать();
	Исключение
		Возврат Ложь;
	КонецПопытки;
	Возврат Истина;
КонецФункции

&НаКлиенте
Процедура СписокПередУдалением(Элемент, Отказ)
	Отказ = Истина;
	Если СписокПередУдалениемНаСервере(Новый Структура("Период,Вопрос", Элемент.ТекущиеДанные.Период, Элемент.ТекущиеДанные.Вопрос)) Тогда
		Элементы.Список.Обновить();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	НаКлиенте.Показать(Элемент.ТекущиеДанные.Вопрос, СтандартнаяОбработка);
КонецПроцедуры
